generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}


model webhook {
  id String @id @default(cuid())
  url String @unique
  createdAt DateTime @default(now())
}